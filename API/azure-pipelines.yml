# ASP.NET Build Pipeline for Self-Hosted Agent
trigger:
- master

pool:
  name: 'Default' # Azure'un sunucuları yerine kendi bilgisayarınızdaki 'Default' havuzunu kullanır

variables:
  solution: '**/*.csproj'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

steps:
- task: NuGetToolInstaller@1

- task: NuGetCommand@2
  inputs:
    restoreSolution: '$(solution)'

- task: VSBuild@1
  inputs:
    solution: '$(solution)'
    msbuildArgs: '/p:DeployOnBuild=true /p:WebPublishMethod=Package /p:PackageAsSingleFile=true /p:SkipInvalidConfigurations=true /p:PackageLocation="$(build.artifactStagingDirectory)"'
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

- task: PublishBuildArtifacts@1
  inputs:
    # $(build.artifactStagingDirectory) kısmındaki eksik parantez düzeltildi
    PathtoPublish: '$(build.artifactStagingDirectory)' 
    ArtifactName: 'backend'
    publishLocation: 'Container'